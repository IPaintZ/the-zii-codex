# Archived mockups

Earlier alternate mockups, kept for reference. **Not wired into the launcher
(`index.html`) and not served as live tools.** Safe to ignore day to day.

The shipped versions that superseded these live in the repo root:

| Archived here | Superseded by (live tool) |
|---|---|
| `alchemy-mockup-1-simple.html` | `alchemy-app.html` (was `alchemy-mockup-2-full.html`) |
| `cook-mockup-A.html` | `cook-app.html` (was `cook-mockup-B.html`) |

Kept in case a layout idea from a variant is useful later. If you ever revive
one, move it back to the root and add it to the `TOOLS` registry in
`index.html`.
